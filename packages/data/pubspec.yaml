name: data
description: Flutter Laba App | Data Layer.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  domain:
    path: ../domain

dev_dependencies:
  build_runner: ^2.1.7
  injectable_generator: ^1.5.3