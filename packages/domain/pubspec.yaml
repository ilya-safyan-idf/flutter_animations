name: domain
description: Flutter Laba | Domain Layer.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  get_it: ^7.2.0
  injectable: ^1.5.3

dev_dependencies:
  build_runner: ^2.1.7
  injectable_generator: ^1.5.3